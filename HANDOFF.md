# CampusNotesAI Handoff

## 프로젝트 개요
- 프로젝트명: `CampusNotesAI`
- 목적: 대학생 대상 AI 기반 노트 앱 캡스톤 MVP
- 현재 스택: `Vite + React + pdfjs-dist`
- 백엔드: 없음
- AI 연동: 없음
- 현재는 프론트엔드 데모/프로토타입 중심 상태

## 현재 구현 상태 요약
### 1. 메인 홈 화면
- 메인 화면과 노트 상세 화면이 분리되어 있음
- 홈 화면은 삼성 노트 느낌의 구조로 정리된 상태
- 좌측 사이드바 메뉴
  - `모든 노트`
  - `즐겨찾기`
  - `휴지통`
  - `폴더`
- `폴더`가 기본 메인 화면
- 폴더 화면에서는 상단에 폴더 카드, 아래에 노트 카드가 보임
- 폴더에 속하지 않은 노트는 별도로 표시됨
- 폴더/노트 데이터는 현재 mock 데이터 기반

### 2. 메인 화면 기능
- `노트 작성` 기능 있음
- `폴더 생성` 기능 있음
- 노트 우클릭 메뉴
  - `삭제`
  - `즐겨찾기 추가/해제`
  - `공유`
- 폴더 우클릭 메뉴
  - `이름변경`
  - `삭제`
- 노트 삭제 시 휴지통으로 이동
- 폴더 삭제 시 폴더와 폴더 내부 노트가 함께 휴지통으로 이동
- 삭제 전 확인 다이얼로그 표시됨

### 3. 노트 상세 화면
- 상세 화면은 `상단 전체 툴바 + 아래 본문 2단 구조`로 정리됨
- 상단 툴바는 화면 전체 폭을 사용함
- 툴바 아래에서만 `좌측 AI Assistant / 우측 PDF·필기 영역` 구조가 시작됨
- 상단 툴바
  - 홈 버튼
  - 노트명
  - `AI` 토글 버튼
  - 필기/타이핑/지우개/캡처/PDF 업로드 등 버튼
- 툴바 디자인
  - 단국대 느낌의 파란색 헤더 톤으로 통일
  - 툴바 버튼은 가운데 정렬
  - 버튼 상태 색상 규칙
    - 비활성: 흰색
    - 활성: 파란색
- 좌측 AI Assistant
  - 툴바 아래 본문 왼쪽에 위치
  - 접기/펼치기 가능
  - 드래그로 너비 조절 가능
  - 최대 폭은 화면 절반 정도
  - 접으면 패널이 완전히 사라지고, 학습자료가 전체 폭을 차지함
- AI 리사이즈 핸들
  - 시각적으로는 거의 보이지 않음
  - 실제 드래그 가능한 hit area는 유지함

### 4. PDF 뷰어 / 필기 영역
- `pdfjs-dist`로 실제 PDF 페이지를 렌더링함
- 페이지가 세로로 쌓여 있고 스크롤로 페이지 이동함
- `Ctrl + 스크롤` 시 PDF 영역만 확대/축소됨
- 확대/축소는 CSS scale 기반
- 기본 데모 PDF는 `public/4_Maximum likelihood learning.pdf`

### 5. 필기 / 메모 기능
- 손글씨 모드 지원
- 타이핑 모드 지원
- 지우개 모드 지원
- 영역 캡처 모드 지원
- 손글씨는 PDF 위에 오버레이 방식으로 구현됨
- 타이핑은 텍스트 박스 방식이 아님
- 각 페이지는 큰 `textarea` 하나를 사용하지만, 클릭 규칙은 줄 단위로 커서 이동하게 커스텀 처리됨

### 6. 현재 타이핑 규칙
- 클릭한 `x` 좌표는 무시됨
- 클릭한 `y` 좌표만 줄 높이 기준으로 계산됨
- 클릭 위치가 `5.5번째 줄` 같은 상태면 `5번째 줄`로 스냅됨
- 클릭 후 커서는 항상 해당 줄의 시작점(0열)으로 이동함
- 즉 사용자가 중간을 클릭해도 그 줄 맨 앞에서 타이핑 시작

### 7. Enter / Backspace 페이지 이동 규칙
- `Enter`
  - 현재 페이지 마지막 줄이 아니면 같은 페이지에서 줄바꿈
  - 현재 페이지 마지막 줄에서만 다음 페이지 첫 줄로 이동
- `Backspace`
  - 같은 페이지에서 첫 줄이 아니면 한 줄 위로 이동
  - 현재 페이지 첫 줄에서만 이전 페이지로 이동
  - 이전 페이지로 이동할 때는 이전 페이지의 기존 입력 상태와 관계없이 항상 마지막 줄 시작점으로 이동
- 이 동작은 사용자가 원하는 노트 필기 흐름 기준으로 여러 번 조정해서 현재 맞춘 상태

### 8. Undo / Redo
- 손글씨, 타이핑 모두 `undo/redo` 대상
- 현재 보고 있는 페이지만이 아니라 노트 전체 편집 기록 기준으로 동작
- 지원 단축키
  - `Ctrl+Z`: undo
  - `Ctrl+Shift+Z`: redo
  - `Ctrl+Y`: redo
- 타이핑 중 `Tab`은 다음 요소 이동이 아니라 들여쓰기로 동작

### 9. 영역 캡처 / 이미지
- `영역 캡처` 모드에서 자유선택 경로를 그릴 수 있음
- 선택한 영역을 이미지로 생성함
- 생성된 캡처는 현재 페이지 아래 미리보기 카드로 쌓임
- 이미지 업로드 기능 있음
- 카메라 입력 기능 있음
- 아직 구현 안 된 것
  - 캡처 결과를 AI Assistant에 바로 전달
  - 캡처 결과 복사/삭제 버튼
  - 캡처 결과를 AI 문맥에 직접 연결

### 10. 저장 / 복원
- 일부 편집 상태는 `localStorage`에 저장됨
- 현재 저장/복원 대상
  - 타이핑 메모
  - 손글씨 stroke
  - redo stroke 상태
  - 노트별 현재 보고 있던 페이지 인덱스

## 현재 의존성
- `react`
- `react-dom`
- `pdfjs-dist`

## 현재 핵심 파일
- `src/App.jsx`
  - 전체 앱 구조
  - 홈 화면 / 노트 상세 화면 분기
  - PDF 렌더링
  - 손글씨/타이핑/지우개/영역캡처
  - Enter/Backspace 줄/페이지 이동 규칙
  - 메인 화면 우클릭 메뉴/삭제 로직
  - AI Assistant UI / 드래그 너비 조절
- `src/styles.css`
  - 전체 레이아웃
  - 홈 화면 UI
  - 노트 상세 화면 UI
  - `note-study-shell`, 내부 AI/툴바/PDF 배치
- `public/4_Maximum likelihood learning.pdf`
  - 기본 데모 PDF
- `package.json`
  - 현재 `pdfjs-dist` 포함

## 오늘 작업 내용
- 노트 상세 화면 상단 구조를 다시 정리함
  - 기존 `왼쪽 AI 헤더 + 오른쪽 툴바` 느낌이 남아 있던 구조를 제거
  - `전체 폭 상단 툴바 + 아래 AI/학습자료` 구조로 통일
- 상단 툴바 스타일 수정
  - 파란색 헤더 톤 적용
  - 툴바 버튼 가운데 정렬
  - 홈 버튼을 아이콘 버튼으로 변경
  - `AI` 버튼 위치를 노트명 오른쪽으로 이동
  - 툴바 버튼 크기 축소
  - 버튼 색상 규칙을 `비활성 흰색 / 활성 파란색`으로 통일
- AI 패널 동작 수정
  - 접으면 완전히 사라지고 학습자료가 남은 폭 전체 사용
- 코드 정리
  - 예전 텍스트 박스 기반 타이핑 구현 흔적 제거
  - 더 이상 쓰지 않는 상수 / 상태 / ref / helper / effect 일부 제거
  - 현재 구조에서 쓰이지 않는 예전 스타일 블록 일부 제거
  - `npm.cmd run build` 정상 통과 확인

## DB / AI 없이 가능한 것
- 필기 도구 확장
  - 펜 색상 / 굵기
  - 형광펜
  - 부분 지우개 / 선택 지우개
- 문서 UX 개선
  - 페이지 썸네일 목록
  - 최근 노트 / 정렬 / 필터 고도화
  - 자동 저장 표시
  - 히스토리 UI
- 캡처 기능 확장
  - 복사 버튼
  - 이미지 저장 버튼
  - 삭제 버튼
- 내보내기 / 로컬 저장
  - 브라우저 localStorage 기반 저장 확장
  - 프론트엔드 단 PDF 주석 export 시도
- 디자인 / 반응형 / 태블릿 UX polish

## DB / AI 없이 하기 어려운 것
- 여러 기기 간 동기화
- 사용자 계정별 노트 / 폴더 / PDF 영구 저장
- 여러 사용자의 데이터 분리 / 공유 링크 / 협업
- 실제 AI 답변 생성
  - 현재는 mock 응답만 가능
- 대화 이력 기반 실제 요약 / 질의응답 / 이미지 분석
- 서버에 파일 업로드 후 안정적으로 다시 불러오기

## DB / AI 연동 시 구현 방향 메모
- DB 연동 시 가능해지는 것
  - 노트 / 폴더 / PDF / 필기 데이터 영구 저장
  - 노트북/데스크톱 간 동기화
  - 사용자별 데이터 관리
- AI 연동 시 가능해지는 것
  - 현재 페이지 + 주변 페이지 + 사용자 메모 + 캡처 이미지 기반 실제 질의응답
  - 요약 / 핵심 정리 / 발표용 질문 응답 생성
- 현재 프로젝트에는 아직 백엔드와 인증이 없으므로, 실제 서비스화하려면 보통 아래 순서가 자연스러움
  1. DB / 파일 저장 구조 설계
  2. 로그인 / 사용자 구분
  3. 노트 저장 API
  4. AI API 연동

## 실행 방법
```powershell
npm.cmd install
npm.cmd run dev
```

## 빌드 확인
```powershell
npm.cmd run build
```
- 현재 빌드는 정상 통과함
- 다만 `pdfjs-dist` 때문에 번들 크기 경고는 있음
- 실패는 아니고 경고만 있는 상태

## 지금 상태에서 특히 중요한 UX 메모
- 타이핑 줄 스냅 / Enter / Backspace 동작은 사용자가 원하는 규칙으로 꽤 세밀하게 맞춘 상태라 건드릴 때 주의 필요
- 노트 상세 화면 레이아웃은 최근에 크게 바뀌었음
  - 지금은 `상단 전체 툴바 + 아래 AI/학습자료` 구조
- AI 리사이즈 핸들은 실제보다 훨씬 얇아 보이게 처리해 둔 상태

## 아직 남아 있는 / 다음 작업 후보
### 우선순위 높음
- 영역 캡처 결과에 `AI로 보내기`, `복사`, `삭제` 기능 붙이기
- 캡처 결과를 AI Assistant 질문 문맥에 연결하기
- 타이핑 서식 기능 구상
  - 글자 크기
  - 글자 색상
  - 정렬
  - 글머리기호

### 우선순위 중간
- AI Assistant mock 응답을 더 발표용 시나리오에 맞게 개선
- 노트 데이터/폴더 데이터 mock 구조를 더 실제 서비스처럼 다듬기
- DB 구조 / 파일 저장 방식 결정
- 실제 AI 연동 전 질문 흐름 UX 정리

### 우선순위 낮음
- 번들 크기 최적화
- README 정리
- HANDOFF.md 지속 업데이트

## 주의사항
- 파일 인코딩은 `UTF-8` 유지 중요
- 실제 수정은 `src` 기준으로 해야 함
- `dist`는 빌드 결과물이라 직접 수정 대상 아님
- 현재는 백엔드가 없고 완전 로컬 프론트엔드 데모 상태
- 지금 `App.jsx`에는 핵심 로직이 많이 몰려 있어, 다음 큰 작업(DB/AI 연동) 전에 파일 분리 리팩터링을 고려해도 좋음

## 다음 Codex에게 바로 넘길 프롬프트 예시
```text
프로젝트 루트의 HANDOFF.md를 먼저 읽고, 현재 구현 상태를 파악한 뒤 이어서 작업해줘.
이 프로젝트는 대학생용 AI 노트앱 캡스톤 MVP이고, 현재는 React + pdfjs-dist 기반 프론트엔드 데모 상태야.

중요한 현재 상태:
- 메인 홈 화면과 노트 상세 화면이 분리되어 있음
- 홈 화면에는 노트 작성 / 폴더 생성 / 우클릭 삭제 기능이 있음
- 노트 상세 화면은 상단 전체 툴바가 있고, 그 아래에 좌측 AI Assistant / 우측 PDF·필기 영역이 있음
- 타이핑은 텍스트 박스 방식이 아니라 줄 단위 스냅 방식
- 클릭한 x좌표는 무시하고 y좌표만 줄 단위로 반영
- Enter는 마지막 줄에서만 다음 페이지로 이동
- Backspace는 첫 줄에서만 이전 페이지로 이동하고, 이전 페이지 마지막 줄로 감

이 규칙은 이미 맞춰둔 상태라 함부로 깨지지 않게 유지하면서 다음 작업을 진행해줘.
```

